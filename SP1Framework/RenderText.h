#pragma once
#ifndef RENDER_TEXT_H
#define RENDER_TEXT_H
#include <fstream>
#include <iostream>
#include <string>

using namespace std;

string WhichText(char entity);

string RenderText(string WhichToRender);

#endif // !RENDER_TEXT_H
